ShipFlow is a small logistics company that handles different shipment types: domestic courier delivery, pickup point delivery, and international delivery. Every shipment follows the same processing flow, but each shipment type calculates its price in a slightly different way.

The goal is to build a Java console application in which the common processing algorithm is written once in an abstract class, while the details are implemented in concrete subclasses.

